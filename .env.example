# Digital Penang LMS — official Frappe Learning Docker wrap

SITE_NAME=lms.iyazbrhm.cloud
ADMIN_PASSWORD=changeme-strong-admin-password
DB_ROOT_PASSWORD=changeme-strong-db-root-password

# Cloudflare Tunnel → http://localhost:8090
HTTP_PUBLISH_PORT=8090
SOCKETIO_PUBLISH_PORT=9000

# 0 = production-like; 1 = developer_mode (official init default)
DEVELOPER_MODE=0
