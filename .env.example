# Digital Penang LMS — Dokploy Environment / local .env
# Image is hardcoded in docker-compose.yml as amirul123/lms-custom:latest
# DELETE any IMAGE_NAME / VERSION vars that point at ghcr.io/frappe/lms

SITE_NAME=lms.iyazbrhm.cloud
FRAPPE_SITE_NAME_HEADER=lms.iyazbrhm.cloud

ADMIN_PASSWORD=changeme-strong-admin-password
DB_ROOT_PASSWORD=changeme-strong-db-root-password

HTTP_PUBLISH_PORT=8090
PULL_POLICY=always

DB_HOST=db
DB_PORT=3306
REDIS_CACHE=redis-cache:6379
REDIS_QUEUE=redis-queue:6379
SOCKETIO_PORT=9000

UPSTREAM_REAL_IP_ADDRESS=127.0.0.1
UPSTREAM_REAL_IP_HEADER=X-Forwarded-For
UPSTREAM_REAL_IP_RECURSIVE=off
PROXY_READ_TIMEOUT=120
CLIENT_MAX_BODY_SIZE=50m
